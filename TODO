* implement error messages
* Match vars >10

* 
Variable "$POSIX_ENGINE" is not imported at /var/src/re-engine-POSIX/blib/lib/re/engine/POSIX.pm line 21.
        (Did you mean &POSIX_ENGINE instead?)

suggest POSIX_ENGINE or POSIX_ENGINE() instead (if it has prototype ())
